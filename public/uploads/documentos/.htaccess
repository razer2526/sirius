# PDFs con datos clínicos: solo se sirven por documento.php (verifica sesión y permisos).
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
