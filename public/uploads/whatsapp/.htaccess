# Adjuntos de WhatsApp (entrantes y salientes): solo se sirven por whatsapp_media.php
# (verifica sesión y acceso a la conversación antes de leer un solo byte).
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
