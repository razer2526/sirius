# Documentos adjuntos de expedientes: solo se sirven por documento_expediente.php
# (verifica sesión y permisos antes de leer un solo byte).
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
